@model IEnumerable<TEST2.Models.Stock>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BranchNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BranchNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Branch">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Branch">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Branch">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
